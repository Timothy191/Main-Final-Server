# Repo Change Index

**Canonical, append-only log of every change to the Arch-System repo.**

Every agent appends one entry per change (one per commit is fine) before
declaring the work done. This is a global rule — see
[`../AGENTS.md`](../AGENTS.md) → "Repo change index — a global rule".

This index is the **temporal** companion to the structural
[`WAYFINDER.md`](./WAYFINDER.md): the wayfinder says _what is here_, the change
index says _how it got here_.

## Entry format

Append to the table, **newest at the top**. One row per change.

| Date       | Agent            | Area          | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Files                                                                                                                                                    | Docs updated                                                               |
| ---------- | ---------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 2026-08-03 | Claude (glm-5.2) | agents        | Ported beneficial slice of `anthropics/knowledge-work-plugins` (a Claude Code plugin marketplace for business roles) as project slash commands: `/deploy-checklist` (on-prem Supabase deploy + cold-cache gates + RLS + migrations + rollback), `/runbook` (writes to `docs/runbooks/` wired to WAYFINDER + change-index), `/adr` (visual→`packages/theme/DECISIONS.md`, non-visual→`docs/architecture/`, per AGENTS.md). Rejected business-role plugins (not software-dev), `engineering/code-review` (duplicate of project code-reviewer agent), `design/design-system` (stricter gated system already exists), and ad-hoc SQL skills (Kysely/migrations used instead). Not installed as a plugin (selective commands cleaner; no MCP connectors). | `.claude/commands/deploy-checklist.md`, `.claude/commands/runbook.md`, `.claude/commands/adr.md`                                                         | `docs/REPO-CHANGE-INDEX.md`, `apps/portal/AGENT_TRACER.md`                 |
| 2026-08-03 | Claude (glm-5.2) | agents        | Ported beneficial Claude-Code agent-infra from `anthropics/claude-cookbooks`: project-tuned `code-reviewer` subagent (`.claude/agents/code-reviewer.md`) encoding Arch-System rules (R2, ACL single-source, errors contract, turbo-cache-masking, change-index) + `/review-pr` slash command (`.claude/commands/review-pr.md`) that invokes it. Rejected the cookbooks hooks (non-standard interface vs canonical stdin-JSON schema) and the Python/notebook content (not applicable to a TS monorepo).                                                                                                                                                                                                                                              | `.claude/agents/code-reviewer.md`, `.claude/commands/review-pr.md`                                                                                       | `docs/REPO-CHANGE-INDEX.md`, `apps/portal/AGENT_TRACER.md`                 |
| 2026-08-03 | Claude (glm-5.2) | tooling       | Consolidated 4 identical config files into symlinks (single canonical each): `packages/rate-limiter/jest.config.js`→`../errors/jest.config.js`; `packages/errors/.eslintrc.cjs`→`../database/.eslintrc.cjs`; `packages/departments/ui/.eslintrc.js`→`../../rate-limiter/.eslintrc.js`; `apps/portal/tailwind.config.ts`→`../../packages/ui/tailwind.config.ts`. Verified ESLint + Jest resolve through symlinks.                                                                                                                                                                                                                                                                                                                                     | `packages/rate-limiter/jest.config.js`, `packages/errors/.eslintrc.cjs`, `packages/departments/ui/.eslintrc.js`, `apps/portal/tailwind.config.ts`        | `docs/REPO-CHANGE-INDEX.md`, `apps/portal/AGENT_TRACER.md`                 |
| 2026-08-03 | Claude (glm-5.2) | design-system | Refined `design-ratchet.mjs` `bgOpacityFill` matcher to the R2-precise target — raw `bg-white`/`bg-black` at opacity ≥20 + `bg-[var(--vibrancy-surface)]`; excludes ≤15 accents (button/input/chip styling) and semantic/primitive token fills (R3, not R2). Re-seeded baseline 159 → 31. Resolves the false-positive regression from concurrent-agent accent additions without touching their WIP.                                                                                                                                                                                                                                                                                                                                                  | `tools/design-ratchet.mjs`, `tools/design-ratchet.baseline.json`                                                                                         | `docs/REPO-CHANGE-INDEX.md`, `apps/portal/AGENT_TRACER.md`                 |
| 2026-08-03 | Claude (glm-5.2) | acl           | Promote department ACL into edge-safe `@repo/acl` package; rewire `proxy.ts` + `dept-access.ts` to import from it (kills the inline-copy drift, incl. missing `tools` key).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `packages/acl/**`, `apps/portal/src/proxy.ts`, `apps/portal/src/lib/dept-access.ts`, `apps/portal/package.json`                                          | `docs/WAYFINDER.md`, `apps/portal/AGENT_TRACER.md`                         |
| 2026-08-03 | Claude (glm-5.2) | cache         | Cache/auth coherence: AGENT-TRACE the 300s employee-auth staleness seam in `proxy.ts`; extend `/api/cache/invalidate` with a `userId` → `cacheEvictL1ByPrefix` hook.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/portal/src/proxy.ts`, `apps/portal/src/app/api/cache/invalidate/route.ts`, `apps/portal/src/lib/department-cache.ts`                               | `docs/WAYFINDER.md`, `apps/portal/AGENT_TRACER.md`                         |
| 2026-08-03 | Claude (glm-5.2) | design-system | Standalone R2 ratchet gate (`design-ratchet.mjs`) + `generated.ts` shape guard (`theme-shape-guard.mjs`); both non-turbo-cached, baseline-grandfathered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tools/design-ratchet.mjs`, `tools/design-ratchet.baseline.json`, `tools/theme-shape-guard.mjs`, `tools/theme-shape-guard.baseline.json`, `package.json` | `docs/design-system/RULES.md` (R7), `docs/WAYFINDER.md`                    |
| 2026-08-03 | Claude (glm-5.2) | meta          | Established the doc-maintenance + change-index global rule and the wayfinder; seeded this index.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `AGENTS.md`, `docs/REPO-CHANGE-INDEX.md`, `docs/WAYFINDER.md`, `CLAUDE.md`                                                                               | `AGENTS.md`, `docs/REPO-CHANGE-INDEX.md`, `docs/WAYFINDER.md`, `CLAUDE.md` |

- **Area** — a wayfinder concept where possible (`acl`, `errors`, `design-system`, `cache`, `portal/auth`, …).
- **Files** — the product files changed in the commit (not docs).
- **Docs updated** — every doc changed in the same commit. `none` is a red flag — if you changed behavior, some doc should reflect it.

## Rules

1. **Never delete or edit a past row** — append only. Corrections go in a new row.
2. **One row per change**, not per file. Group a commit's files into one entry.
3. **Before "done"** — if the row is missing, the change is not done.
